"""Generates a fictional company + role + tasks + planned twist.

Implemented in Phase 2 — one strong-tier call with structured JSON output,
validated against a schema, retried once on malformed output.
"""
from __future__ import annotations

# Implemented in Phase 2.
