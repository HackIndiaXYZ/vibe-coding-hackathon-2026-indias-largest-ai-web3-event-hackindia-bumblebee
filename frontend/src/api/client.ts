/**
 * REST client for the FastAPI backend. Implemented in Phase 1 / wired in Phase 6.
 * Dev requests are proxied via Vite (/api → http://localhost:8000), so callers
 * use relative paths like fetch("/api/sessions").
 */
export {};
